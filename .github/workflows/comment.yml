name: Issue Comment

on:
  issue_comment:
    types: [created]

jobs:
  issue_comment:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Run script
      run: |
        # Your script here
        # You can use ${{ github.event.comment.body }} to access the comment body
        # You can use ${{ github.event.issue.number }} to access the issue number
